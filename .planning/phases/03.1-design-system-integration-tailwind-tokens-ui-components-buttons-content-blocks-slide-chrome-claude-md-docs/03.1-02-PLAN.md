---
phase: 03.1-design-system-integration
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/components/ui/SlideFrame/SlideFrame.tsx
  - src/components/ui/SlideFrame/StopBadge.tsx
  - src/components/ui/SlideFrame/SubSlideProgress.tsx
  - src/components/ui/SlideFrame/index.ts
  - src/components/ui/SlideFrame/__tests__/SlideFrame.test.tsx
  - src/components/ui/index.ts
  - src/components/features/slide/SlideOverlay/SlideOverlay.tsx
  - src/components/features/slide/SlideOverlay/SlideNavArrows.tsx
  - src/components/features/presentation/PresentationFooter.tsx
autonomous: true
requirements:
  - DS-SLIDEFRAME
  - DS-REFACTOR

must_haves:
  truths:
    - "Slide overlay renders with grey-wash background, white inner card, red stud-pattern header bar"
    - "Close button in SlideFrame has id='slide-close-btn' — FocusTrap initialFocus preserved"
    - "Nav arrows in slide are LEGO brick-style buttons (grey left, red right)"
    - "PresentationFooter uses Button component for back/toggle/advance controls"
    - "ARIA live region and FocusTrap in SlideOverlay are preserved after refactor"
    - "StopBadge renders red pill with 'STOP N' and label"
    - "SubSlideProgress shows current/visited/upcoming dot states"
    - "All existing SlideOverlay tests still pass (regression)"
  artifacts:
    - path: "src/components/ui/SlideFrame/SlideFrame.tsx"
      provides: "Chrome wrapper: stud-pattern header + grey-wash outer + white inner card + nav arrow slots"
      exports: ["SlideFrame"]
    - path: "src/components/ui/SlideFrame/StopBadge.tsx"
      provides: "Red pill badge showing STOP N and label"
      exports: ["StopBadge"]
    - path: "src/components/ui/SlideFrame/SubSlideProgress.tsx"
      provides: "Dot row showing current/visited/upcoming sub-slide progress"
      exports: ["SubSlideProgress"]
    - path: "src/components/features/slide/SlideOverlay/SlideOverlay.tsx"
      provides: "Refactored to compose SlideFrame instead of SlideHeader"
    - path: "src/components/features/slide/SlideOverlay/SlideNavArrows.tsx"
      provides: "Refactored to use Button component with brick-style arrows"
    - path: "src/components/features/presentation/PresentationFooter.tsx"
      provides: "Refactored to use Button component for back/toggle/advance"
  key_links:
    - from: "src/components/features/slide/SlideOverlay/SlideOverlay.tsx"
      to: "src/components/ui/SlideFrame/SlideFrame.tsx"
      via: "import { SlideFrame } from '@/components/ui'"
      pattern: "SlideFrame"
    - from: "src/components/ui/SlideFrame/SlideFrame.tsx"
      to: "id='slide-close-btn'"
      via: "Close button must preserve this id for FocusTrap initialFocus in SlideOverlay"
      pattern: "slide-close-btn"
    - from: "src/components/features/presentation/PresentationFooter.tsx"
      to: "src/components/ui/Button/Button.tsx"
      via: "import { Button } from '@/components/ui'"
      pattern: "Button"
---

<objective>
Build the SlideFrame chrome wrapper (stud header, grey-wash background, white card, nav arrow slots) with StopBadge and SubSlideProgress sub-components, then refactor SlideOverlay, SlideNavArrows, and PresentationFooter to consume the new UI primitives.

Purpose: This transforms the slide overlay from dark-theme placeholder to the LEGO light-theme design, and brings PresentationFooter's buttons into the brick-depth design system.
Output: SlideFrame with sub-components, refactored SlideOverlay/SlideNavArrows/PresentationFooter, all tests green.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/assets/styleguide.md
@.planning/phases/03.1-design-system-integration-tailwind-tokens-ui-components-buttons-content-blocks-slide-chrome-claude-md-docs/03.1-CONTEXT.md
@.planning/phases/03.1-design-system-integration-tailwind-tokens-ui-components-buttons-content-blocks-slide-chrome-claude-md-docs/03.1-RESEARCH.md

Source files to refactor:
@src/components/features/slide/SlideOverlay/SlideOverlay.tsx
@src/components/features/slide/SlideOverlay/SlideHeader.tsx
@src/components/features/slide/SlideOverlay/SlideNavArrows.tsx
@src/components/features/slide/SlideOverlay/SlideContent.tsx
@src/components/features/presentation/PresentationFooter.tsx
@src/components/features/slide/OverlayPresence.tsx

Test reference:
@src/components/features/slide/SlideOverlay/__tests__/SlideOverlay.test.tsx

<interfaces>
<!-- Button component created in Plan 01 -->
From src/components/ui/Button/Button.tsx (created in Plan 01):
```typescript
'use client'
type ButtonVariant = 'yellow' | 'red' | 'grey'
type ButtonSize = 'icon' | 'label'
interface ButtonProps {
  variant: ButtonVariant
  size?: ButtonSize
  onClick?: () => void
  disabled?: boolean
  'aria-label'?: string
  children: React.ReactNode
  className?: string
}
export function Button(props: ButtonProps): JSX.Element
```

From src/components/ui/index.ts (created in Plan 01):
```typescript
export * from './Button'
```

<!-- Existing SlideOverlay contracts to preserve -->
From src/components/features/slide/SlideOverlay/SlideOverlay.tsx:
- FocusTrap with focusTrapOptions={{ initialFocus: '#slide-close-btn', returnFocusOnDeactivate: false }}
- ARIA live region: <div aria-live="polite" aria-atomic="true" className="sr-only">
- data-testid="slide-overlay" on the motion.div
- Uses usePresentation() for state, dispatch, triggerRef

From src/components/features/slide/SlideOverlay/__tests__/SlideOverlay.test.tsx:
- Tests check: data-testid="slide-overlay", [data-focus-trap], [aria-live="polite"], button with name /close/i and id="slide-close-btn", stops[0].slides[0].heading text
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SlideFrame chrome wrapper + StopBadge + SubSlideProgress</name>
  <files>src/components/ui/SlideFrame/SlideFrame.tsx, src/components/ui/SlideFrame/StopBadge.tsx, src/components/ui/SlideFrame/SubSlideProgress.tsx, src/components/ui/SlideFrame/index.ts, src/components/ui/SlideFrame/__tests__/SlideFrame.test.tsx, src/components/ui/index.ts</files>
  <behavior>
    - SlideFrame renders a close button with id="slide-close-btn" and aria-label="Close slide overlay"
    - SlideFrame renders a header element with the stud-pattern class
    - SlideFrame renders children inside a white content area
    - SlideFrame renders leftArrow and rightArrow props when provided
    - SlideFrame renders a LEGO text placeholder in the header (left side)
    - StopBadge renders "STOP {N}" text where N is stopIndex + 1
    - StopBadge renders the stop label text
    - SubSlideProgress renders the correct number of dots (equal to total prop)
    - SubSlideProgress marks the current dot as wider/active
  </behavior>
  <action>
**Test file first** (`SlideFrame/__tests__/SlideFrame.test.tsx`):
Test SlideFrame:
- Renders close button with `id="slide-close-btn"` (CRITICAL for FocusTrap — this is a regression-sensitive ID)
- Close button has `aria-label="Close slide overlay"`
- Header element has `stud-pattern` class
- Children render inside the component
- leftArrow and rightArrow slots render when provided

Test StopBadge:
- Renders "STOP 1" for stopIndex=0
- Renders the label text

Test SubSlideProgress:
- Renders correct number of span elements for total=3
- Current dot (index matches current prop) has wider width

**SlideFrame component** (`SlideFrame/SlideFrame.tsx`):
- NOT `'use client'` — SlideFrame is layout-only, no hooks. Parent (SlideOverlay) is already 'use client'.
- Props: `onClose: () => void`, `leftArrow?: React.ReactNode`, `rightArrow?: React.ReactNode`, `children: React.ReactNode`
- Structure (from RESEARCH.md Pattern 4):
  ```
  <div> (grey-wash outer: bg-lego-grey-wash, absolute inset-0, flex flex-col)
    <header> (red stud bar: bg-lego-red stud-pattern, flex items-center justify-between, px-6 py-3)
      <span> LEGO logo placeholder (red rounded badge with "LEGO" text, like current but styled for light theme)
      <button id="slide-close-btn"> Close X (40x40, rounded-md, text-white, hover:bg-white/20, focus-visible ring)
    </header>
    <div> (white card: bg-white, flex-1, rounded-t-xl, relative, overflow-hidden, shadow-lg)
      {leftArrow — absolute left-4, top-1/2, -translate-y-1/2, z-10}
      {rightArrow — absolute right-4, top-1/2, -translate-y-1/2, z-10}
      <div> (content area: px-16 py-8, h-full, overflow-y-auto, flex flex-col items-center justify-center)
        {children}
      </div>
    </div>
  </div>
  ```
- Close button SVG: same X icon from current SlideHeader (18x6 to 6x18 cross lines)

**StopBadge** (`SlideFrame/StopBadge.tsx`):
- Props: `stopIndex: number`, `stopLabel: string`
- Red pill badge: `bg-lego-red text-white font-display font-bold text-sm uppercase tracking-wide rounded-pill px-3 py-1`
- Content: `STOP {stopIndex + 1} · {stopLabel}`

**SubSlideProgress** (`SlideFrame/SubSlideProgress.tsx`):
- Props: `total: number`, `current: number`
- `aria-hidden="true"` (decorative)
- Map total dots: current = wider pill (22x8px, bg-lego-red), visited (i < current) = 8x8 circle bg-lego-red opacity-45, upcoming = 8x8 circle bg-lego-grey-pale

**Barrel exports:**
- `SlideFrame/index.ts`: export SlideFrame, StopBadge, SubSlideProgress
- Update `ui/index.ts`: add `export * from './SlideFrame'`
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npx vitest run src/components/ui/SlideFrame</automated>
  </verify>
  <done>SlideFrame renders grey-wash outer + red stud header + white card + nav arrow slots. Close button has id="slide-close-btn". StopBadge shows red pill. SubSlideProgress shows dot states. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor SlideOverlay, SlideNavArrows, and PresentationFooter to use design system</name>
  <files>src/components/features/slide/SlideOverlay/SlideOverlay.tsx, src/components/features/slide/SlideOverlay/SlideNavArrows.tsx, src/components/features/presentation/PresentationFooter.tsx</files>
  <action>
**SlideOverlay.tsx refactor:**
- Remove import of SlideHeader (will be replaced by SlideFrame)
- Import `SlideFrame`, `StopBadge`, `SubSlideProgress` from `@/components/ui`
- Import Button from `@/components/ui` (for nav arrow buttons passed to SlideFrame)
- KEEP: FocusTrap wrapper with EXACT same focusTrapOptions (initialFocus: '#slide-close-btn', returnFocusOnDeactivate: false)
- KEEP: motion.div wrapper with data-testid="slide-overlay" and EXACT same initial/animate/exit/transition props
- KEEP: ARIA live region (`<div aria-live="polite" aria-atomic="true" className="sr-only">`) as the FIRST child of motion.div
- CHANGE: Remove `bg-slate-900 text-white flex flex-col` from motion.div className. Keep `absolute inset-0 z-40`. The SlideFrame provides its own background.
- CHANGE: Replace SlideHeader + content wrapper with:
  ```
  <SlideFrame
    onClose={handleClose}
    leftArrow={<Button variant="grey" size="icon" onClick={() => dispatch({ type: 'BACK' })} aria-label="Previous slide"><LeftChevronSVG /></Button>}
    rightArrow={<Button variant="red" size="icon" onClick={() => dispatch({ type: 'ADVANCE' })} aria-label="Next slide"><RightChevronSVG /></Button>}
  >
    <StopBadge stopIndex={state.currentStop} stopLabel={stop.label} />
    <SubSlideProgress total={stop.slides.length} current={state.currentSlide} />
    <SlideContent heading={slide.heading} lines={slide.lines} />
  </SlideFrame>
  ```
- Keep SlideContent import and usage — Phase 4 owns that component (but update its text colors from white to dark since theme is now light)
- The chevron SVGs can be inline in the JSX (same arrow icons currently in SlideNavArrows)
- SlideNavArrows.tsx is no longer imported by SlideOverlay — the nav arrows are now inline Button components passed to SlideFrame slots. SlideNavArrows.tsx file can remain (for potential future use) but is no longer imported here.

**SlideNavArrows.tsx update:**
- Refactor to use Button component from `@/components/ui` instead of raw HTML buttons
- Left arrow: `<Button variant="grey" size="icon">` with left chevron SVG
- Right arrow: `<Button variant="red" size="icon">` with right chevron SVG
- Keep the same props interface (onBack, onAdvance)
- Note: SlideOverlay no longer imports this file directly (it inlines the buttons), but keep it updated for potential reuse

**PresentationFooter.tsx refactor:**
- Import `Button` from `@/components/ui`
- Back button: Replace raw `<button>` with `<Button variant="grey" size="icon" onClick={handleBack} aria-label="Previous">` containing the left chevron SVG
- Toggle button (Map/Zoom In): Replace raw `<button>` with `<Button variant="yellow" size="label" onClick={handleToggle}>` containing the map/zoom icon + text
- Advance button: Replace raw `<button>` with `<Button variant="red" size="icon" onClick={handleAdvance} aria-label="Next">` containing the right chevron SVG
- KEEP: Progress track section (the stop number buttons and connecting lines) UNCHANGED. Phase 03.3 redesigns this.
- KEEP: The footer wrapper element and overall layout
- Update footer background: use `bg-white` (already is), ensure `shadow-md` for footer bar shadow per styleguide

**SlideContent.tsx: DO NOT MODIFY THIS FILE.** Phase 4 owns it. But note that SlideContent currently uses white text (for dark theme). Since SlideFrame is now light-themed, the executor should verify that the heading and body text are readable. If SlideContent's hardcoded `text-white` classes make text invisible on white bg, the executor should update ONLY the color classes in SlideContent (text-white -> text-lego-dark, text-white/80 -> text-lego-grey, text-white/90 -> text-lego-dark, bg-white/10 -> bg-lego-grey-wash) as a minimal fix. This is a theme compatibility fix, NOT a Phase 4 content redesign.

**CRITICAL REGRESSION CHECKS:**
- After refactoring, run existing SlideOverlay tests. They must ALL pass:
  - data-testid="slide-overlay" must still exist
  - [data-focus-trap] must still wrap the overlay
  - [aria-live="polite"] must still contain stop label text
  - Button with name /close/i must have id="slide-close-btn"
  - stops[0].slides[0].heading text must render
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npx vitest run</automated>
  </verify>
  <done>SlideOverlay uses SlideFrame (light theme, stud header, white card). Nav arrows are brick-style Buttons (grey back, red forward). PresentationFooter uses Button component. All 5 existing SlideOverlay tests pass. Full test suite green. SlideContent text is readable on light background.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite passes (existing SlideOverlay tests + new SlideFrame tests + new Button tests)
- `npx next build` — static export succeeds
- SlideOverlay renders with grey-wash background, white card, red stud-pattern header
- Close button id="slide-close-btn" preserved (FocusTrap regression)
- ARIA live region preserved in SlideOverlay (A11Y-02 regression)
- FocusTrap preserved in SlideOverlay (A11Y-01 regression)
- PresentationFooter has brick-depth buttons (grey back, yellow toggle, red advance)
</verification>

<success_criteria>
- SlideFrame: grey-wash outer, red stud header, white inner card, nav arrow slots
- StopBadge: red pill with "STOP N" and label
- SubSlideProgress: current/visited/upcoming dot states
- SlideOverlay refactor: uses SlideFrame, FocusTrap intact, ARIA live intact, close button id intact
- PresentationFooter refactor: uses Button component for all 3 controls
- All tests green (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system-integration-tailwind-tokens-ui-components-buttons-content-blocks-slide-chrome-claude-md-docs/03.1-02-SUMMARY.md`
</output>
