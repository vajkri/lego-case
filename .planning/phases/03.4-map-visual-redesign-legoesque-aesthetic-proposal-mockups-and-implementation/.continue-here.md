---
phase: 03.4-map-visual-redesign
task: 1
total_tasks: 3
status: in_progress
last_updated: "2026-03-14T16:13:34.426Z"
---

<current_state>
Phase 03.4 is in the "implement chosen direction" stage. User reviewed the HTML mockup (map-redesign-proposals.html) with 3 full scene directions + per-element proposals, then picked a mix-and-match set. The first round of implementation is done. User gave 3 feedback items (mountains z-order, straight horizon, road edges) which have been applied but NOT yet visually verified by the user. Dev server is running on localhost:3000.
</current_state>

<completed_work>

- Context gathered (03.4-CONTEXT.md) — committed
- HTML mockup created (.planning/assets/mockups/map-redesign-proposals.html) with 3 directions (Blueprint, Brick by Brick, LEGO Land) + per-element proposals
- User selected mix-and-match elements and first implementation pass applied:
  - Grass: baseplate stud pattern overlay via SVG `<pattern>` ✓
  - Sun: warm glow halos + 4 studs ✓
  - Mountains: replaced with stepped brick pyramids ✓
  - Buildings: roof studs on all 4 buildings (no brick courses) ✓
  - Road: drop shadow removed, edge lines added ✓
- First feedback round applied (not yet verified by user):
  - Mountains moved after grass in SVG render order (z-index fix) ✓
  - Grass horizon changed from curvy Q-bezier to straight rect at y=355 ✓
  - Road edge lines changed from faint white to solid dark charcoal (#2A2E38, width 44) ✓
</completed_work>

<remaining_work>

- Visual verification of feedback round 1 (mountains z-order, straight horizon, road edges)
- Likely more feedback rounds on visual tweaks
- Late-phase tasks (after main redesign is approved):
  1. Ambient animations — sun pulse, cloud drift, windmill rotation (CSS transform/opacity, prefers-reduced-motion)
  2. Shadow cleanup audit across all map elements
  3. Car stop alignment — adjust STOP_OFFSETS in RoadPath.tsx after map changes
- Commit all map visual changes
</remaining_work>

<decisions_made>

- Mockup-first approach with HTML proposal file — worked well for getting user feedback
- User rejected all 3 full directions but liked individual elements from each
- Mix-and-match choices:
  - Grass stud pattern (from Direction B)
  - Sun: Direction C glow + Direction B studs
  - Mountains: Direction B stepped bricks
  - Building roof studs only (Direction B), no brick course lines
  - Road: shadow removed, edge lines (from "Prove It" section)
  - Everything else unchanged (trees, clouds, windmill, pond, fence, sky)
- Straight grass horizon (y=355) — more LEGO-like than curvy
- Road edges: dark charcoal border (#2A2E38) not white — thicker (44 vs 34) and darker
</decisions_made>

<blockers>
None
</blockers>

<context>
This phase is iterative — user reviews visually, gives feedback, repeat. The main visual choices are locked (which elements to change). Now in the refinement loop adjusting sizes, positions, and appearance until the user is happy. After main redesign approval, 3 polish tasks remain (animations, shadow audit, car alignment).

Files modified but uncommitted: MapSvg.tsx (mountains, grass, sun, building studs), RoadPath.tsx (shadow removed, edge lines). globals.css and PresentationFooter.tsx also show as modified in git status but those are from phase 03.3 work, not this phase.
</context>

<next_action>
Start with: Ask user for visual feedback on the 3 changes (mountains z-order, straight horizon, road edges). Then continue the feedback loop until main redesign is approved. After approval, proceed to late-phase tasks (animations, shadow audit, car alignment).
</next_action>
