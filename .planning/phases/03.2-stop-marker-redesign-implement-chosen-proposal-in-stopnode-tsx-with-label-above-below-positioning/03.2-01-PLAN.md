---
phase: 03.2-stop-marker-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/presentation.ts
  - src/data/topics/stop-01-the-case.ts
  - src/data/topics/stop-02-vision.ts
  - src/data/topics/stop-03-how-we-work.ts
  - src/data/topics/stop-04-migration.ts
  - src/data/topics/stop-05-summary.ts
  - src/data/topics/__tests__/stops.test.ts
  - src/components/features/map/__tests__/StopNode.test.tsx
autonomous: true
requirements:
  - MAP-02
  - A11Y-03

must_haves:
  truths:
    - "Stop type includes a required labelPosition field with 'above' or 'below' values"
    - "All 5 stop data files have the correct labelPosition value per spec"
    - "TypeScript compilation succeeds with no errors after type extension"
    - "StopNode tests assert 5 visual states via data-state attribute"
    - "StopNode tests assert focus outline style for A11Y-03"
  artifacts:
    - path: "src/types/presentation.ts"
      provides: "Stop interface with labelPosition field"
      contains: "labelPosition: 'above' | 'below'"
    - path: "src/data/topics/__tests__/stops.test.ts"
      provides: "labelPosition validation test"
      contains: "labelPosition"
    - path: "src/components/features/map/__tests__/StopNode.test.tsx"
      provides: "5-state and focus outline test assertions"
      contains: "data-state"
  key_links:
    - from: "src/types/presentation.ts"
      to: "src/data/topics/stop-*.ts"
      via: "Stop type import"
      pattern: "import type.*Stop.*from.*presentation"
---

<objective>
Extend the Stop type with labelPosition, update all 5 stop data files, and extend test scaffolds to cover the new 5-state system and A11Y-03 focus indicator.

Purpose: Establishes the data contract and test expectations before the visual rewrite in Plan 02. Type + data changes must land together to avoid TypeScript errors.
Output: Updated type, 5 updated data files, extended test files (tests will be RED until Plan 02 implements the new StopNode).
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-stop-marker-redesign-implement-chosen-proposal-in-stopnode-tsx-with-label-above-below-positioning/03.2-CONTEXT.md
@.planning/phases/03.2-stop-marker-redesign-implement-chosen-proposal-in-stopnode-tsx-with-label-above-below-positioning/03.2-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From src/types/presentation.ts (current):
```typescript
export interface Stop {
  slug: string
  label: string
  coordinates: { x: number; y: number }
  slides: Slide[]
}
```

From src/data/topics/stop-01-the-case.ts (example pattern):
```typescript
import type { Stop } from '@/types/presentation'
export const stopTheCase: Stop = {
  slug: 'the-case',
  label: 'The Case',
  coordinates: { x: 10, y: 85 },
  slides: [ /* ... */ ],
}
```

From src/data/topics/__tests__/stops.test.ts (existing test pattern):
```typescript
import { stops } from '../index'
// Tests iterate stops array with for..of loops
```

From src/components/features/map/__tests__/StopNode.test.tsx (existing mock):
```typescript
const mockStop: Stop = {
  slug: 'test-stop',
  label: 'Test Stop',
  coordinates: { x: 50, y: 50 },
  slides: [{ heading: 'Heading', lines: ['Line 1'] }],
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Stop type and update all 5 data files with labelPosition</name>
  <files>src/types/presentation.ts, src/data/topics/stop-01-the-case.ts, src/data/topics/stop-02-vision.ts, src/data/topics/stop-03-how-we-work.ts, src/data/topics/stop-04-migration.ts, src/data/topics/stop-05-summary.ts</files>
  <action>
    1. In `src/types/presentation.ts`, add `labelPosition: 'above' | 'below'` to the `Stop` interface, after the `coordinates` field. Add a JSDoc comment: `/** Manual label positioning above or below the brick marker -- set per stop to avoid road overlap */`.

    2. Update each stop data file to add the `labelPosition` field (after `coordinates`, before `slides`):
       - `stop-01-the-case.ts`: `labelPosition: 'above'`
       - `stop-02-vision.ts`: `labelPosition: 'above'`
       - `stop-03-how-we-work.ts`: `labelPosition: 'below'`
       - `stop-04-migration.ts`: `labelPosition: 'above'`
       - `stop-05-summary.ts`: `labelPosition: 'above'`

    These values are locked decisions from CONTEXT.md.

    3. Do NOT make `labelPosition` optional -- it must be a required field so TypeScript enforces that no stop is accidentally omitted.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Stop interface has required labelPosition field. All 5 data files compile with correct values. TypeScript reports zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test scaffolds for 5-state system, labelPosition, and A11Y-03 focus</name>
  <files>src/data/topics/__tests__/stops.test.ts, src/components/features/map/__tests__/StopNode.test.tsx</files>
  <action>
    1. In `src/data/topics/__tests__/stops.test.ts`, add a new test inside the existing `describe('stops data')` block:

    ```typescript
    it('every stop has a valid labelPosition', () => {
      for (const stop of stops) {
        expect(['above', 'below']).toContain(stop.labelPosition)
      }
    })
    ```

    2. In `src/components/features/map/__tests__/StopNode.test.tsx`:

    a. Update `mockStop` to include `labelPosition: 'above'` (required by updated Stop type).

    b. Add a new describe block `'StopNode -- 5 visual states via data-state'` with tests asserting:
       - Default state (not active, not visited): `data-state="default"`
       - Visited state (not active, is visited): `data-state="visited"`
       - Active state (is active): `data-state="active"`
       - Note: hover and focus states are user-interaction driven and tested below.

    c. Add a new describe block `'StopNode -- A11Y-03: focus indicator'` with a test:
       - Render StopNode with default state
       - Find the button, simulate focus via `await user.tab()` or `fireEvent.focus(button)`
       - Assert that the marker wrapper (or button itself) has an outline style applied. Since the new implementation uses CSS `outline: 3px solid #0055BF` with `outline-offset: 4px`, assert the rendered DOM has the focus styling. In jsdom, checking for focus-related class application or style attribute presence is sufficient.
       - Note: This test may be RED until Plan 02 implements the new StopNode -- that is expected and acceptable.

    d. Update the existing `describe('StopNode -- MAP-03: three visual states')` heading to `'StopNode -- MAP-03: visual state distinction'` and ensure the `data-state` comparison test still works (active vs visited different `data-state` values).

    Important: The existing test `'active stop is visually distinct from visited stop (different classes)'` should continue to work because the new StopNode will still use different `data-state` attributes per state.
  </action>
  <verify>
    <automated>npx vitest run src/data/topics/__tests__/stops.test.ts</automated>
  </verify>
  <done>Stops data test has labelPosition assertion (GREEN). StopNode test file has 5-state data-state assertions and A11Y-03 focus test (may be RED until Plan 02 -- that is Wave 0 expected behavior).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors (type + data consistency)
- `npx vitest run src/data/topics/__tests__/stops.test.ts` passes (labelPosition test GREEN)
- `npx vitest run src/components/features/map/__tests__/StopNode.test.tsx` -- existing tests pass; new 5-state tests may be RED (expected, Plan 02 dependency)
</verification>

<success_criteria>
- Stop type has required `labelPosition: 'above' | 'below'` field
- All 5 stop data files updated with correct values per CONTEXT.md spec
- TypeScript compiles cleanly
- Stops data test validates labelPosition for every stop
- StopNode test file extended with data-state and focus outline assertions
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-stop-marker-redesign-implement-chosen-proposal-in-stopnode-tsx-with-label-above-below-positioning/03.2-01-SUMMARY.md`
</output>
