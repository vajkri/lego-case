---
phase: 03.2-stop-marker-redesign
plan: "02"
subsystem: ui
tags: [svg, animation, css, stop-marker, brick-marker, lego, map, a11y]

# Dependency graph
requires:
  - phase: 03.2-01
    provides: Stop.labelPosition field, 5-state data-state test scaffold
provides:
  - BrickMarker SVG component with 5 visual states (default/hover/focus/active/visited)
  - brick-drop @keyframe animation in globals.css with prefers-reduced-motion guard
  - StopNode rewritten: MarkerPin removed, BrickMarker installed, label above/below positioning
  - All 12 StopNode tests GREEN (previously 11/12)
affects:
  - Phase 03.3 (progress track redesign — uses same visual language)
  - Phase 03.4 (map visual redesign — builds on stop marker)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - BrickState type union ('default' | 'hover' | 'focus' | 'active' | 'visited') for 5-state component logic
    - Module-level SVG coordinate constants co-located with the component that uses them
    - CSS fill transition on SVG <rect> style prop (not attribute) for color sweep animation
    - brick-drop-in CSS class for entrance animation (not JS animation library)
    - flex-col / flex-col-reverse for label-above/below positioning pattern
    - Inline style objects per variant (not Tailwind) for exact rgba/filter values not representable as tokens

key-files:
  created: []
  modified:
    - src/components/features/map/StopNode.tsx
    - src/app/globals.css

key-decisions:
  - "MarkerPin removed entirely; BrickMarker is the complete replacement — brick-stack SVG is the new visual language"
  - "brick-drop animation applied only to topmost brick <g> element, not whole SVG, for correct enter effect"
  - "CSS fill transition (300ms ease) on all rect style props enables active-to-visited color sweep without JS animation"
  - "Label positioning uses flex-col (above) / flex-col-reverse (below) per stop.labelPosition — no absolute positioning needed"
  - "Key prop added to stud <g> wrapper instead of fragment to fix React list warning"

patterns-established:
  - "SVG coordinate constants at module top level — POS, STUD, COLORS, BRICK_LAYOUTS — single source of truth matched to mockup"
  - "BrickState priority: active > focus > hover > visited > default — mirrors expected UX priority"
  - "Inline style objects for all state-variant styling (not Tailwind classes) when values contain rgba/filter that Tailwind v4 cannot tokenize"

requirements-completed: [MAP-02, MAP-03, A11Y-03]

# Metrics
duration: ~6min
completed: 2026-03-14
tasks_completed: 1
tasks_total: 2
files_modified: 2
---

# Phase 03.2 Plan 02: Stop Marker Implementation Summary

**BrickMarker SVG component replacing MarkerPin — 5-state LEGO brick stack (1-5 bricks per stop) with brick-drop entrance animation, fill-transition color sweep, and above/below label positioning.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-03-14T12:31:20Z
- **Completed:** 2026-03-14T12:37:00Z
- **Tasks:** 1/2 complete (Task 2 is checkpoint:human-verify, paused for visual confirmation)
- **Files modified:** 2

## Accomplishments

- Rewrote `StopNode.tsx` from scratch: `MarkerPin` SVG removed, `BrickMarker` component added with full 5-state system
- `BrickMarker` renders 1–5 bricks per stop index using half-width brick layout system (matching mockup exactly)
- 5 visual states with distinct styling: default (grey), hover (scale+shadow), focus (blue outline), active (LEGO palette), visited (all-green)
- Color sweep active→visited via `style={{ fill: ..., transition: 'fill 300ms ease' }}` on all SVG `<rect>` elements
- Brick-drop entrance animation: `brick-drop-in` CSS class applied to topmost brick `<g>` when `state === 'active'`
- `@keyframes brick-drop` added to `globals.css` with `prefers-reduced-motion` media query guard
- Label positioned above/below via `flex-col` / `flex-col-reverse` based on `stop.labelPosition`
- All 12 StopNode tests GREEN (the previously intentionally RED `data-state="default"` test now passes)
- Full test suite: 114/114 GREEN

## Task Commits

1. **Task 1: Add brick-drop keyframe and rewrite StopNode with BrickMarker** - `97b7c41` (feat)

**Plan metadata:** (pending — awaiting checkpoint:human-verify)

## Files Created/Modified

- `src/components/features/map/StopNode.tsx` — Complete rewrite: BrickMarker SVG + 5-state StopNode (325 lines added, 49 removed)
- `src/app/globals.css` — Added `@keyframes brick-drop`, `.brick-drop-in` class, `prefers-reduced-motion` guard

## Decisions Made

- `MarkerPin` removed entirely — no backward compatibility stub needed since BrickMarker is a complete replacement with the same external API
- CSS fill transition on rect `style` prop (not SVG `fill` attribute) — required for CSS transitions to work reliably on SVG elements
- `brick-drop-in` class on topmost brick `<g>` only (not entire SVG) — ensures only the newest brick animates in, not the whole stack
- Label positioning uses `flex-col`/`flex-col-reverse` — simpler and more maintainable than absolute positioning
- React `key` prop on `<g>` wrapper for stud pairs (not fragment) — fragments cannot carry key props in this JSX context

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed React key prop warning in stud list rendering**
- **Found during:** Task 1 (test run output)
- **Issue:** `studXPositions.map()` returned a `<>` fragment with `key` on inner elements — React warns keys should be on the outermost element of the mapped item
- **Fix:** Changed `<>` to `<g key={...}>` as the mapped item wrapper; removed key from inner `<rect>` elements
- **Files modified:** `src/components/features/map/StopNode.tsx`
- **Verification:** Test run after fix shows no React warnings in stderr
- **Committed in:** 97b7c41 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug fix)
**Impact on plan:** Necessary correctness fix. No scope creep.

## Issues Encountered

None — plan executed cleanly. TypeScript errors from `npx tsc --noEmit` are all pre-existing issues in `__tests__/` files (vitest globals not configured for tsc); zero errors in production source files.

## Next Phase Readiness

- Task 2 (checkpoint:human-verify) requires visual inspection at `http://localhost:3000` to confirm all 5 states match mockup
- After checkpoint approval: SUMMARY closed, STATE.md updated, Phase 03.3 (progress track redesign) can begin
- No changes needed in `MapCanvas.tsx` — StopNode API unchanged (same props, same export)

---

*Phase: 03.2-stop-marker-redesign*
*Completed: 2026-03-14*
