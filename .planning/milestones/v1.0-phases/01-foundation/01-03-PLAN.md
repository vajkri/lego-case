---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/data/topics/stop-01-the-case.ts
  - src/data/topics/stop-02-vision.ts
  - src/data/topics/stop-03-how-we-work.ts
  - src/data/topics/stop-04-migration.ts
  - src/data/topics/stop-05-summary.ts
  - src/data/topics/index.ts
autonomous: true
requirements:
  - FOUND-06
  - QUAL-02

must_haves:
  truths:
    - "All 5 stop data files exist with full slide content sourced from proposal-content.md"
    - "The `stops[]` barrel export contains exactly 5 typed Stop entries"
    - "The data layer test from Plan 02 passes (GREEN state)"
    - "No hardcoded content strings appear in component JSX — all content lives in data files"
  artifacts:
    - path: "src/data/topics/stop-01-the-case.ts"
      provides: "Stop 1: 2 slides (Why We Are Here, Goals)"
    - path: "src/data/topics/stop-02-vision.ts"
      provides: "Stop 2: 3 slides (Frontend Architecture, Component Architecture, Design System Strategy)"
    - path: "src/data/topics/stop-03-how-we-work.ts"
      provides: "Stop 3: 5 slides (Team Structure, Storybook, CI/CD, Testing, Privacy-First Analytics)"
    - path: "src/data/topics/stop-04-migration.ts"
      provides: "Stop 4: 3 slides (Incremental Route Migration, Migration Steps, Risk Mitigation)"
    - path: "src/data/topics/stop-05-summary.ts"
      provides: "Stop 5: 1 slide (Summary)"
    - path: "src/data/topics/index.ts"
      provides: "Barrel: exports stops[] array and Stop type"
      exports:
        - "stops"
  key_links:
    - from: "src/data/topics/index.ts"
      to: "src/types/presentation.ts"
      via: "import type { Stop }"
      pattern: "import.*Stop.*from.*@/types/presentation"
    - from: "src/data/topics/__tests__/stops.test.ts"
      to: "src/data/topics/index.ts"
      via: "import { stops }"
      pattern: "stops.*toHaveLength.*5"
---

<objective>
Author all 5 stop data files with full prose content sourced directly from `.planning/assets/proposal-content.md`. This turns the RED test from Plan 02 GREEN and satisfies FOUND-06 (content-complete data files).

Purpose: All slide content must live in typed data files — zero hardcoded strings in component JSX (QUAL-02). This plan creates the single source of truth for all presentation content.
Output: 5 stop files + barrel index; `npm test` passes.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/assets/proposal-content.md

<interfaces>
<!-- Types defined in Plan 02 — use these exactly. Do not redefine. -->
From src/types/presentation.ts:
```typescript
export interface Slide {
  heading: string
  lines: string[]
}

export interface Stop {
  slug: string
  label: string
  coordinates: {
    x: number  // % of SVG viewport width
    y: number  // % of SVG viewport height
  }
  slides: Slide[]
}
```

Placeholder coordinates (will be replaced in Phase 3):
- Stop 1: { x: 15, y: 50 }
- Stop 2: { x: 30, y: 35 }
- Stop 3: { x: 50, y: 55 }
- Stop 4: { x: 70, y: 40 }
- Stop 5: { x: 85, y: 50 }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 stop data files from proposal-content.md</name>
  <files>
    src/data/topics/stop-01-the-case.ts,
    src/data/topics/stop-02-vision.ts,
    src/data/topics/stop-03-how-we-work.ts,
    src/data/topics/stop-04-migration.ts,
    src/data/topics/stop-05-summary.ts
  </files>
  <action>
Read `.planning/assets/proposal-content.md` first. Source all slide content from it — do not invent content.

Create `src/data/topics/` directory if needed.

**Content map (proposal-content.md section → slide):**

| Stop | File | Source sections | Slides |
|------|------|-----------------|--------|
| 1 | stop-01-the-case.ts | §13 (AngularJS context), §1 Goals | 2 |
| 2 | stop-02-vision.ts | §2 Frontend Architecture, §4 Component Architecture, §5 Design System Strategy | 3 |
| 3 | stop-03-how-we-work.ts | §3 Team Structure, §6 Storybook, §7 CI/CD, §8 Testing, §10-12 Analytics | 5 |
| 4 | stop-04-migration.ts | §13 Migration Strategy, §14 Migration Steps, §15 Risk Mitigation | 3 |
| 5 | stop-05-summary.ts | §16 Summary | 1 |

**Required shape for every file:**

```typescript
import type { Stop } from '@/types/presentation'

export const stop[Name]: Stop = {
  slug: '...',
  label: '...',
  coordinates: { x: N, y: N },
  slides: [
    { heading: '...', lines: ['...', '...'] },
  ],
}
```

**Rules:**
- Every `lines[]` entry is a complete, standalone sentence or bullet point from the source doc
- No truncation — capture the full substance of each source section
- Do not invent content not present in `proposal-content.md`
- `coordinates` use the placeholder values from the interface block above
- slugs: `'the-case'`, `'vision'`, `'how-we-work'`, `'migration'`, `'summary'`
- labels: `'The Case'`, `'Vision'`, `'How We Work'`, `'Migration'`, `'Summary'`

**Stop 1 reference implementation (follow this pattern for all 5):**

```typescript
// src/data/topics/stop-01-the-case.ts
import type { Stop } from '@/types/presentation'

export const stopTheCase: Stop = {
  slug: 'the-case',
  label: 'The Case',
  coordinates: { x: 15, y: 50 },
  slides: [
    {
      heading: 'Why We Are Here',
      lines: [
        'kids.lego.com currently runs on AngularJS — end of life',
        'No modern tooling, no SSR, no type safety',
        'Migration is a business continuity issue, not just a tech upgrade',
      ],
    },
    {
      heading: 'Goals',
      lines: [
        'Deliver fast, engaging experiences for children aged 6–12',
        'Support multiple teams working in parallel',
        'Maintain high performance globally and carbon-aware delivery',
        'Focus on inclusivity and accessibility for children',
        'Enable rapid campaign and content updates',
        'Ensure privacy-first analytics compliant with COPPA and GDPR',
        'Support emerging markets: low bandwidth, resilient media, localization',
      ],
    },
  ],
}
```

Author the remaining 4 files following the same pattern, sourcing content from the corresponding sections of proposal-content.md.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 5 stop files exist. `npx tsc --noEmit` exits 0 — every file satisfies the `Stop` type. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel export and turn tests GREEN</name>
  <files>src/data/topics/index.ts</files>
  <action>
Create `src/data/topics/index.ts` that assembles the 5 stop files into the exported `stops[]` array.

```typescript
// src/data/topics/index.ts
// Single source of truth for all presentation stops.
// Import this array wherever stop data is needed — never import individual stop files directly.
import type { Stop } from '@/types/presentation'
import { stopTheCase } from './stop-01-the-case'
import { stopVision } from './stop-02-vision'
import { stopHowWeWork } from './stop-03-how-we-work'
import { stopMigration } from './stop-04-migration'
import { stopSummary } from './stop-05-summary'

export const stops: Stop[] = [
  stopTheCase,
  stopVision,
  stopHowWeWork,
  stopMigration,
  stopSummary,
]
```

After creating this file, run `npm test` (without `--passWithNoTests`) and confirm all 5 tests in `stops.test.ts` pass.

The test file was authored in Plan 02 and verifies: array length = 5, each stop has slug/label/slides, coordinates are valid percentages, all slides have heading + lines, slugs are unique.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 tests in `src/data/topics/__tests__/stops.test.ts` pass (GREEN). `npm run build` still exits 0.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/krisztinavajda/dev/lego-case && npm test && npm run build
```

Confirm:
1. All 5 data tests pass
2. Build still succeeds
3. `src/data/topics/` contains 6 files (5 stops + index.ts)
4. No TypeScript errors
</verification>

<success_criteria>
- 5 stop files exist at `src/data/topics/stop-0{1-5}-*.ts`
- `src/data/topics/index.ts` exports `stops: Stop[]` with 5 elements
- `npm test` exits 0 with all 5 assertions green
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` following the summary template.
</output>
