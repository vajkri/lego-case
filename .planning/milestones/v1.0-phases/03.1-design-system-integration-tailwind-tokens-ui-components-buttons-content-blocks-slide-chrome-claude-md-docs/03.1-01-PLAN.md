---
phase: 03.1-design-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/ui/Button/Button.tsx
  - src/components/ui/Button/index.ts
  - src/components/ui/Button/__tests__/Button.test.tsx
  - src/components/ui/index.ts
autonomous: true
requirements:
  - DS-TOKENS
  - DS-FONTS
  - DS-BUTTON

must_haves:
  truths:
    - "All LEGO design tokens from styleguide.md are available as Tailwind utility classes"
    - "Baloo 2 and DM Sans fonts load via next/font with zero FOUC"
    - "Button component renders yellow, red, and grey variants with brick depth"
    - "Stud dot pattern CSS class exists and is usable"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Full LEGO token set in @theme block + rgba tints in :root + stud-pattern utility class"
      contains: "--color-lego-red"
    - path: "src/app/layout.tsx"
      provides: "next/font imports for Baloo 2 and DM Sans with CSS variable classes on html element"
      contains: "Baloo_2"
    - path: "src/components/ui/Button/Button.tsx"
      provides: "Button component with 3 variants (yellow/red/grey), 2 sizes (icon/label), brick depth"
      exports: ["Button"]
    - path: "src/components/ui/Button/__tests__/Button.test.tsx"
      provides: "Unit tests for Button variant rendering"
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for UI primitives"
      exports: ["Button"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variables --font-baloo2 and --font-dm-sans referenced in @theme --font-display and --font-body"
      pattern: "var\\(--font-baloo2\\)"
---

<objective>
Define the LEGO design system foundation: all tokens in Tailwind v4 @theme, Google Fonts via next/font, stud dot pattern utility, and the Button component with brick-depth variants.

Purpose: Every subsequent plan (SlideFrame, content blocks, refactors) depends on these tokens and the Button primitive existing. This is the foundation layer.
Output: globals.css with full token set, layout.tsx with font loading, Button component with tests, ui/ barrel export.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/assets/styleguide.md

Source files to modify:
@src/app/globals.css
@src/app/layout.tsx
@src/components/ui/index.ts

<interfaces>
<!-- Current globals.css is a placeholder with just --color-brand and --font-display -->
<!-- Current layout.tsx has no font imports, plain <html lang="en"> -->
<!-- Current ui/index.ts is an empty placeholder comment -->

From src/app/layout.tsx (current):
```typescript
import type { Metadata } from 'next'
import { PresentationProvider, KeyboardController, PresentationFooter } from '@/components/features/presentation'
import { OverlayPresence } from '@/components/features/slide'
import './globals.css'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <PresentationProvider>
          <KeyboardController />
          <div className="flex flex-col h-screen">
            <div className="flex-1 relative overflow-hidden">
              <OverlayPresence />
              {children}
            </div>
            <PresentationFooter />
          </div>
        </PresentationProvider>
      </body>
    </html>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens in globals.css + next/font in layout.tsx</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
**globals.css** — Replace the entire file content. Keep the existing @import and @source directives. Replace the placeholder @theme block with the full LEGO token set from styleguide.md section 1:

In the `@theme` block:
- Fonts: `--font-display: var(--font-baloo2), sans-serif` and `--font-body: var(--font-dm-sans), sans-serif`
- Primary colors: `--color-lego-red: #E3000B`, `--color-lego-red-dark: #A00008`, `--color-lego-yellow: #F5CD2D`, `--color-lego-yellow-dark: #D4A800`, `--color-lego-blue: #0055BF`, `--color-lego-green: #00A850`
- Neutrals: `--color-lego-dark: #2C2C2C`, `--color-lego-grey: #6D6E70`, `--color-lego-grey-light: #A0A1A3`, `--color-lego-grey-pale: #D4D4D8`, `--color-lego-grey-wash: #F0EEE8`, `--color-lego-cream: #FAF8F2`
- Radii: `--radius-sm: 6px`, `--radius-md: 10px`, `--radius-lg: 12px`, `--radius-xl: 16px`, `--radius-pill: 999px`
- Shadows: `--shadow-sm`, `--shadow-md`, `--shadow-lg` with values from styleguide
- Border widths: `--border-thin: 1.5px`, `--border-default: 3px`, `--border-depth: 4px` (NOTE: these are informational tokens; actual border widths in components use explicit Tailwind values)

In a `:root {}` block (NOT in @theme, because rgba values are not supported in @theme):
- Tints: `--lego-red-tint`, `--lego-red-tint-md`, `--lego-yellow-tint`, `--lego-yellow-tint-md`
- Depth shadows: `--depth-default`, `--depth-red`, `--depth-yellow`
- Stud pattern: `--stud-pattern: radial-gradient(...)`

After `:root`, add the `.stud-pattern` utility class:
```
.stud-pattern {
  background-image: var(--stud-pattern);
  background-size: 32px 32px;
}
```

Use the exact values from the 03.1-RESEARCH.md code examples for all tokens.

**layout.tsx** — Add next/font imports:
- Import `Baloo_2` and `DM_Sans` from `next/font/google`
- Configure `baloo2` with subsets: ['latin'], weight: ['500', '600', '700', '800'], variable: '--font-baloo2', display: 'swap'
- Configure `dmSans` with subsets: ['latin'], weight: ['400', '500', '600'], style: ['normal', 'italic'], variable: '--font-dm-sans', display: 'swap'
- Apply font variable classes to the html element: `className={\`${baloo2.variable} ${dmSans.variable}\`}`
- Keep ALL existing layout structure (PresentationProvider, KeyboardController, OverlayPresence, PresentationFooter, the flex h-screen wrapper) exactly as-is. Only change the html element className.

**Verify token-to-utility generation:** After writing both files, run `npx next build` to confirm the static export still succeeds with the new tokens. If `rounded-pill` or `font-display` utilities don't generate correctly, document the issue but do NOT change the token definitions — downstream tasks will use fallback syntax if needed.
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npx vitest run && npx next build</automated>
  </verify>
  <done>globals.css has full LEGO token set (13 colors, 5 radii, 3 shadows, 4 tints, 3 depth shadows, stud pattern). layout.tsx loads Baloo 2 + DM Sans via next/font. Build succeeds. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button component with brick-depth variants</name>
  <files>src/components/ui/Button/Button.tsx, src/components/ui/Button/__tests__/Button.test.tsx, src/components/ui/Button/index.ts, src/components/ui/index.ts</files>
  <behavior>
    - Button renders with data-testid="button"
    - Yellow variant: has bg-lego-yellow class and border-lego-yellow-dark
    - Red variant: has bg-lego-red class and border-lego-red-dark
    - Grey variant: has bg-lego-grey-wash class and border-lego-grey-pale
    - Icon size: has w-12 and h-12 classes
    - Label size: has h-12 class
    - Disabled state: has opacity-40 and cursor-not-allowed, button is disabled
    - Renders children content
    - Has aria-label when provided
    - Has type="button" by default (not submit)
  </behavior>
  <action>
**Test file first** (`Button/__tests__/Button.test.tsx`):
Write tests asserting the behaviors listed above. Use `@testing-library/react` render + screen. Assert on className includes expected classes (use `toHaveClass` or check className string contains). Do NOT assert on computed CSS styles (jsdom limitation). Mock nothing — Button is a pure presentational component with no hooks.

**Button component** (`Button/Button.tsx`):
- Mark `'use client'` (has onClick handler)
- Props interface: `variant: 'yellow' | 'red' | 'grey'`, `size?: 'icon' | 'label'` (default: 'label'), `onClick?: () => void`, `disabled?: boolean`, `'aria-label'?: string`, `children: React.ReactNode`, `className?: string` (for composition)
- Variant class map: yellow uses `bg-lego-yellow text-lego-dark border-b-4 border-lego-yellow-dark`, red uses `bg-lego-red text-white border-b-4 border-lego-red-dark`, grey uses `bg-lego-grey-wash text-lego-grey border-b-4 border-lego-grey-pale`
- All variants get: `rounded-lg font-display font-extrabold transition-all` + hover brightness + active press (`active:translate-y-0.5 active:border-b-2`)
- Icon size: `w-12 h-12 flex items-center justify-center`
- Label size: `h-12 px-[22px] flex items-center gap-2`
- Disabled: `opacity-40 cursor-not-allowed` — remove hover/active when disabled
- Set `type="button"` on the native button element
- Add `data-testid="button"`

**Barrel exports:**
- `Button/index.ts`: `export * from './Button'`
- `ui/index.ts`: Replace placeholder comment with `export * from './Button'`
  </action>
  <verify>
    <automated>cd /Users/krisztinavajda/dev/lego-case && npx vitest run src/components/ui/Button</automated>
  </verify>
  <done>Button component renders all 3 variants with correct Tailwind classes, both sizes work, disabled state applied. Tests pass. Barrel exports in place.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all existing tests (45+) plus new Button tests pass
- `npx next build` — static export succeeds with new tokens and font imports
- globals.css contains `--color-lego-red`, `--color-lego-yellow`, all 5 radii, all 3 shadows
- layout.tsx has `Baloo_2` and `DM_Sans` imports, className applied to html
- Button component exists with yellow/red/grey variants
</verification>

<success_criteria>
- Design tokens: All 13 color tokens, 5 radius tokens, 3 shadow tokens are in @theme
- Tints and depth shadows: 4 tint + 3 depth shadow values in :root (not @theme)
- Stud pattern: .stud-pattern class in globals.css
- Fonts: Baloo 2 (500-800) + DM Sans (400-600+italic) load via next/font, CSS vars on html
- Button: 3 variants x 2 sizes with brick-depth press effect, tests green
- Build: next build passes, vitest passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-design-system-integration-tailwind-tokens-ui-components-buttons-content-blocks-slide-chrome-claude-md-docs/03.1-01-SUMMARY.md`
</output>
