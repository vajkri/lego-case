---
phase: 04-content-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "next build completes with zero errors and zero warnings"
    - "No console errors or warnings in the production build"
    - "All 5 stops render correctly with content blocks in the slide overlay"
    - "Multi-block slides scroll when content exceeds the slide frame"
    - "App loads and is interactive in under 3 seconds"
  artifacts:
    - path: "out/"
      provides: "Static export output directory"
    - path: "out/index.html"
      provides: "Entry point for the static site"
  key_links:
    - from: "next build"
      to: "out/"
      via: "static export"
      pattern: "output.*export"
---

<objective>
Verify the production build is clean and the full presentation runs through all 5 stops without broken slides, placeholder content, or visual regressions.

Purpose: Satisfy PERF-01 (clean build), PERF-02 (interactive in <3s), and PERF-03 (no console errors) — the final quality gate before the presentation is ready to give.

Output: Verified production build, visual confirmation of all 18 slides.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-and-polish/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification</name>
  <files></files>
  <action>
    Run the full verification pipeline:

    1. **Full test suite:** `npm test` — all tests must pass (excluding pre-existing CarElement/MinifigHead failures which are out of Phase 4 scope). Specifically verify:
       - `src/data/topics/__tests__/stops.test.ts` — all pass
       - `src/components/features/slide/SlideOverlay/__tests__/SlideOverlay.test.tsx` — all pass (SlideOverlay still renders heading from stops data)

    2. **Production build:** `npm run build` — must exit 0 with no errors and no warnings related to static export, server components, or image optimization. The `out/` directory must be generated.

    3. **Console error check:** Start the production server with `npx serve out -l 3333` (or similar static file server), then verify no console-level issues exist by checking the build output logs for warnings.

    4. **If any errors or warnings are found:** Fix them. Common issues:
       - TypeScript errors in data files -> fix the data shape
       - Missing 'use client' directive -> add where needed
       - Import resolution errors -> fix import paths
       - Build warnings about unused exports -> remove dead code

    5. **Bundle size sanity check:** Verify no unexpectedly large bundles in the build output. Phase 4 adds no new packages — only data content. If the JS bundle grew significantly, investigate.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | tail -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm test passes (data + SlideOverlay tests green), npm run build exits 0 with no errors/warnings, out/ directory contains index.html, no unexpected bundle size growth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual walkthrough of all 18 slides</name>
  <files></files>
  <action>
    Present the complete presentation to the user for visual verification. Start the dev server and guide the user through all 5 stops and 18 slides.
  </action>
  <verify>User approves the full presentation walkthrough</verify>
  <done>All 18 slides render correctly, no placeholder content, no visual regressions, human approved.</done>
  <what-built>Complete presentation with all 18 slides across 5 stops, rendered through the new block dispatcher using the LEGO design system content blocks (BulletList, TwoColumnCards, EntityCards, NumberedSteps, CalloutBox, DataTable).</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:3000 in Chrome
    3. You should see the LEGO world map with 5 stops

    **Full run-through (press ArrowRight/Space to advance through everything):**

    4. **Stop 1 — The Case (4 slides):**
       - Slide 1 "Why We Are Here": yellow CalloutBox at top + red-tinted BulletList below
       - Slide 2 "Business Requirements": 4 yellow TwoColumnCards in a 2x2 grid
       - Slide 3 "Assumptions": default BulletList with grey dots
       - Slide 4 "What Success Looks Like": DataTable with metrics + yellow CalloutBox

    5. **Stop 2 — Vision (4 slides):**
       - Slide 1 "Why React + Next.js": 4 default TwoColumnCards
       - Slide 2 "Experience-First Architecture": yellow BulletList + CalloutBox
       - Slide 3 "Component Architecture": yellow NumberedSteps (3 steps)
       - Slide 4 "Design System Strategy": 2 default TwoColumnCards + CalloutBox

    6. **Stop 3 — How We Work (5 slides):**
       - Slide 1 "Team Structure": yellow EntityCards (3 teams) + CalloutBox
       - Slide 2 "Storybook as Component Registry": default BulletList + DataTable
       - Slide 3 "CI/CD & Release Strategy": red NumberedSteps
       - Slide 4 "Testing Strategy": DataTable + CalloutBox
       - Slide 5 "Privacy-First Analytics": red BulletList + 2 default TwoColumnCards

    7. **Stop 4 — Migration (3 slides):**
       - Slide 1 "Migration Strategy": CalloutBox + default BulletList
       - Slide 2 "Migration Steps": yellow NumberedSteps (4 steps)
       - Slide 3 "Risk Mitigation": red EntityCards (3 risks)

    8. **Stop 5 — Summary (2 slides):**
       - Slide 1 "What We Are Proposing": yellow BulletList (5 takeaways)
       - Slide 2 "Questions?": single CalloutBox

    **Check for:**
    - No placeholder/dummy text remaining
    - All block types render with correct visual treatment (tints, borders, depth shadows)
    - Multi-block slides are scrollable if content overflows
    - Text is readable (16px minimum body, correct fonts)
    - Car animation between stops still works
    - Keyboard navigation (ArrowRight/ArrowLeft/Escape) works throughout
    - No console errors in DevTools
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with specific slides</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — PERF-01
2. Production build loads in <3s — PERF-02 (verified during human walkthrough)
3. No console errors in DevTools — PERF-03 (verified during human walkthrough)
4. Full run-through of all 18 slides produces no broken content — PERF-01/PERF-03
</verification>

<success_criteria>
- Production build is clean (zero errors, zero warnings)
- All 18 slides render correctly with appropriate content blocks
- No placeholder content, no broken slides, no visual regressions
- App is interactive in under 3 seconds
- No console errors in production
- Human approves the full presentation walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-and-polish/04-02-SUMMARY.md`
</output>
