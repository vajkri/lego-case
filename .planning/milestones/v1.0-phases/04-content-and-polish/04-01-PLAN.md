---
phase: 04-content-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/presentation.ts
  - src/components/ui/content-blocks/BulletList.tsx
  - src/components/features/slide/SlideOverlay/SlideContent.tsx
  - src/components/features/slide/SlideOverlay/SlideOverlay.tsx
  - src/data/topics/__tests__/stops.test.ts
  - src/data/topics/stop-01-the-case.ts
  - src/data/topics/stop-02-vision.ts
  - src/data/topics/stop-03-how-we-work.ts
  - src/data/topics/stop-04-migration.ts
  - src/data/topics/stop-05-summary.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "Slide type accepts typed content blocks instead of flat string arrays"
    - "SlideContent renders each block type using the correct UI component"
    - "All 18 slides across 5 stops are authored with rich content using the 6 block types"
    - "Tests pass with the new Slide shape"
    - "No TypeScript errors in any modified file"
  artifacts:
    - path: "src/types/presentation.ts"
      provides: "ContentBlock discriminated union + updated Slide interface"
      contains: "type ContentBlock"
    - path: "src/components/features/slide/SlideOverlay/SlideContent.tsx"
      provides: "Block dispatcher rendering"
      exports: ["SlideContent"]
    - path: "src/data/topics/stop-01-the-case.ts"
      provides: "Stop 1: The Case — 4 slides with blocks"
    - path: "src/data/topics/stop-02-vision.ts"
      provides: "Stop 2: Vision — 4 slides with blocks"
    - path: "src/data/topics/stop-03-how-we-work.ts"
      provides: "Stop 3: How We Work — 5 slides with blocks"
    - path: "src/data/topics/stop-04-migration.ts"
      provides: "Stop 4: Migration — 3 slides with blocks"
    - path: "src/data/topics/stop-05-summary.ts"
      provides: "Stop 5: Summary — 2 slides with blocks"
  key_links:
    - from: "src/types/presentation.ts"
      to: "src/components/ui/content-blocks/variants.ts"
      via: "BlockVariant import"
      pattern: "import.*BlockVariant.*from.*variants"
    - from: "src/components/features/slide/SlideOverlay/SlideContent.tsx"
      to: "src/components/ui"
      via: "import of all 6 content block components"
      pattern: "import.*BulletList.*TwoColumnCards.*from.*@/components/ui"
    - from: "src/components/features/slide/SlideOverlay/SlideOverlay.tsx"
      to: "src/components/features/slide/SlideOverlay/SlideContent.tsx"
      via: "blocks prop instead of lines prop"
      pattern: "blocks=\\{slide\\.blocks\\}"
    - from: "src/data/topics/stop-01-the-case.ts"
      to: "src/types/presentation.ts"
      via: "ContentBlock type shapes in blocks arrays"
      pattern: "blocks:.*type:.*'bullet-list'|'callout'|'two-column-cards'"
---

<objective>
Extend the Slide type system with a ContentBlock discriminated union, rewrite the SlideContent renderer as a block dispatcher, and author all 18 slides across 5 stops with rich presentation content.

Purpose: Replace the flat `lines: string[]` slide model with typed content blocks that map 1:1 to the 6 existing UI components (BulletList, TwoColumnCards, EntityCards, NumberedSteps, CalloutBox, DataTable). Then author all presentation content per the locked slide structure from CONTEXT.md.

Output: Fully typed slide data, working block rendering pipeline, all 18 slides authored with real content.
</objective>

<execution_context>
@/Users/krisztinavajda/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krisztinavajda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-and-polish/04-CONTEXT.md
@.planning/phases/04-content-and-polish/04-RESEARCH.md
@.planning/assets/proposal-content.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/presentation.ts (CURRENT — to be modified):
```typescript
export interface Slide {
  heading: string
  lines: string[]  // REMOVE — replace with blocks: ContentBlock[]
}

export interface Stop {
  slug: string
  label: string
  coordinates: { x: number; y: number }
  labelPosition: 'above' | 'below'
  slides: Slide[]
}
```

From src/components/ui/content-blocks/variants.ts:
```typescript
export type BlockVariant = 'default' | 'red' | 'yellow'
export const variantStyleMap: Record<BlockVariant, React.CSSProperties>
```

From src/components/ui/content-blocks/ (all 6 component prop interfaces):
```typescript
// BulletList — heading MUST become optional (heading?: string)
interface BulletListProps { heading: string; items: string[]; variant?: BlockVariant }

// TwoColumnCards
interface TwoColumnCardsProps { cards: { title: string; description: string }[]; variant?: BlockVariant }

// EntityCards
interface EntityCardsProps { entities: { initials: string; title: string; description: string }[]; variant?: BlockVariant }

// NumberedSteps
interface NumberedStepsProps { steps: { title: string; description: string }[]; variant?: BlockVariant }

// CalloutBox — no variant prop, always yellow
interface CalloutBoxProps { children: React.ReactNode }

// DataTable
interface DataTableProps { headers: string[]; rows: string[][]; variant?: BlockVariant }
```

From src/components/features/slide/SlideOverlay/SlideOverlay.tsx (line 104 — integration point):
```typescript
<SlideContent heading={slide.heading} lines={slide.lines} />
// Change to: <SlideContent heading={slide.heading} blocks={slide.blocks} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type contracts + block dispatcher + test updates</name>
  <files>
    src/types/presentation.ts,
    src/components/ui/content-blocks/BulletList.tsx,
    src/components/features/slide/SlideOverlay/SlideContent.tsx,
    src/components/features/slide/SlideOverlay/SlideOverlay.tsx,
    src/data/topics/__tests__/stops.test.ts
  </files>
  <action>
    **1. Extend Slide type in src/types/presentation.ts:**
    - Import `BlockVariant` from `@/components/ui/content-blocks/variants` (variants.ts has no React runtime import in the type definition, so this is safe for a types file)
    - Add a `ContentBlock` discriminated union type with 6 members:
      - `{ type: 'bullet-list'; heading?: string; items: string[]; variant?: BlockVariant }`
      - `{ type: 'two-column-cards'; cards: { title: string; description: string }[]; variant?: BlockVariant }`
      - `{ type: 'entity-cards'; entities: { initials: string; title: string; description: string }[]; variant?: BlockVariant }`
      - `{ type: 'numbered-steps'; steps: { title: string; description: string }[]; variant?: BlockVariant }`
      - `{ type: 'callout'; text: string }` (no variant — CalloutBox is always yellow)
      - `{ type: 'data-table'; headers: string[]; rows: string[][]; variant?: BlockVariant }`
    - Export `ContentBlock`
    - Replace `Slide.lines: string[]` with `Slide.blocks: ContentBlock[]`
    - Keep all other types (Stop, PresentationState, Action) unchanged

    **2. Make BulletList heading optional in src/components/ui/content-blocks/BulletList.tsx:**
    - Change `heading: string` to `heading?: string` in BulletListProps
    - Wrap the `<h3>` element in a conditional: `{heading && (<h3 ...>{heading}</h3>)}`
    - No other changes to BulletList

    **3. Rewrite SlideContent as block dispatcher in src/components/features/slide/SlideOverlay/SlideContent.tsx:**
    - Keep `'use client'` directive
    - Import all 6 content block components from `@/components/ui`
    - Import `ContentBlock` type from `@/types/presentation`
    - Create a `renderBlock(block: ContentBlock, index: number)` function with a switch on `block.type`:
      - `'bullet-list'` -> `<BulletList key={index} heading={block.heading} items={block.items} variant={block.variant} />`
      - `'two-column-cards'` -> `<TwoColumnCards key={index} cards={block.cards} variant={block.variant} />`
      - `'entity-cards'` -> `<EntityCards key={index} entities={block.entities} variant={block.variant} />`
      - `'numbered-steps'` -> `<NumberedSteps key={index} steps={block.steps} variant={block.variant} />`
      - `'callout'` -> `<CalloutBox key={index}>{block.text}</CalloutBox>`
      - `'data-table'` -> `<DataTable key={index} headers={block.headers} rows={block.rows} variant={block.variant} />`
    - SlideContentProps changes from `{ heading: string; lines: string[] }` to `{ heading: string; blocks: ContentBlock[] }`
    - Render: heading as `<h2>` with `font-display font-bold text-[30px] text-lego-dark leading-tight tracking-tight` (per CLAUDE.md typography scale — NOT text-4xl/40px), then map blocks
    - Add `overflow-y-auto` to the outer container to prevent multi-block slides from overflowing the slide frame
    - Layout: `flex-1 flex flex-col gap-6 w-full max-w-4xl mx-auto px-4 py-8 overflow-y-auto`

    **4. Update SlideOverlay.tsx integration point:**
    - Line 104: change `<SlideContent heading={slide.heading} lines={slide.lines} />` to `<SlideContent heading={slide.heading} blocks={slide.blocks} />`
    - No other changes to SlideOverlay.tsx

    **5. Update stops.test.ts:**
    - Change the test "every slide has a heading and at least one content line" to "every slide has a heading and at least one content block"
    - Replace `slide.lines.length` assertions with `slide.blocks.length`
    - Replace the line-content loop with a block-type assertion: `expect(block.type).toBeTruthy()` for each block
    - Add a test verifying every block type is one of the 6 known values: `['bullet-list', 'two-column-cards', 'entity-cards', 'numbered-steps', 'callout', 'data-table']`
    - Do NOT add strict per-stop slide count assertions (brittle)
    - Keep all other existing tests unchanged (slugs, coordinates, labelPosition)

    **Important:** After this task, the 5 stop data files will have TypeScript errors because they still use `lines`. Task 2 fixes all 5 data files. Run `npm test -- src/data/topics/__tests__/stops.test.ts` AFTER Task 2 completes (not after Task 1 alone).
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <done>ContentBlock union type exported from presentation.ts, Slide.lines replaced with Slide.blocks, BulletList heading is optional, SlideContent dispatches to 6 block components, SlideOverlay passes blocks prop, test assertions updated for blocks shape. TypeScript errors expected only in stop data files (fixed in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Author all 18 slides across 5 stops</name>
  <files>
    src/data/topics/stop-01-the-case.ts,
    src/data/topics/stop-02-vision.ts,
    src/data/topics/stop-03-how-we-work.ts,
    src/data/topics/stop-04-migration.ts,
    src/data/topics/stop-05-summary.ts
  </files>
  <action>
    Rewrite all 5 stop data files with the full 18-slide content using ContentBlock types. Each file imports `Stop` from `@/types/presentation`. Keep slug, label, coordinates, and labelPosition unchanged. Replace the `slides` array entirely.

    **Content voice:** Use "we" voice throughout. Sharpen for presentation impact — punchy, active-voice delivery. Source from proposal-content.md + case assignment. Author NEW content for "Why React + Next.js" and "Experience-First Architecture" slides (not in proposal-content.md).

    **Content blocks are pure data — NO JSX, NO React imports in data files.** CalloutBox content is `{ type: 'callout', text: '...' }`.

    **Block sub-headings rule:** Single-block slides omit block-level heading (no `heading` prop). Multi-block slides: add sub-headings only when two blocks of the same type need disambiguation. Different block types are self-distinguishing.

    **Ensure all TwoColumnCards have even card counts (2 or 4).**

    ---

    **Stop 1: The Case (4 slides)**

    Slide 1 — "Why We Are Here":
    - CalloutBox: problem statement about AngularJS end-of-life, business continuity framing
    - BulletList variant:red: 3-4 bullets on current pain points (no modern tooling, no SSR, no TypeScript, fighting the framework)

    Slide 2 — "Business Requirements":
    - TwoColumnCards variant:yellow: 4 cards covering the pillars (fast engaging experiences for kids 6-12, multi-team parallel work, global performance + carbon-aware, privacy-first COPPA/GDPR analytics)

    Slide 3 — "Assumptions":
    - BulletList variant:default: key assumptions per CONTEXT.md specifics:
      - Two engineering teams (Platform + Content/Campaign) to start
      - Existing Node.js/Express.js backend and headless CMS (WordPress) in place
      - Hosting from Billund co-location datacenter
      - Design team will deliver LEGO-specific visual guidelines
      - Safety & Privacy team defines COPPA/GDPR-K compliance requirements

    Slide 4 — "What Success Looks Like":
    - DataTable variant:default: success metrics table (Metric | Target columns — user retention D1/D7/D30, session depth, engagement rate, page load time)
    - CalloutBox: key shift message ("We stop measuring pages viewed and start measuring play experiences completed")

    ---

    **Stop 2: Vision (4 slides)**

    Slide 1 — "Why React + Next.js" [NEW CONTENT]:
    - TwoColumnCards variant:default: 4 cards covering framework benefits mapped to kids.lego.com needs (SSR/SSG/ISR for global performance, App Router for nested layouts, React ecosystem + TypeScript for developer velocity, built-in i18n + image optimization for emerging markets)

    Slide 2 — "Experience-First Architecture" [NEW CONTENT]:
    - BulletList variant:yellow: how Server Components + client interactivity split supports playful animated experiences without compromising low-end device performance
    - CalloutBox: key insight about shipping less JavaScript to the browser while keeping rich interactivity

    Slide 3 — "Component Architecture":
    - NumberedSteps variant:yellow: 3 steps (Build locally -> Document in Storybook -> Promote to shared when reused)

    Slide 4 — "Design System Strategy":
    - TwoColumnCards variant:default: 2 cards (Shared layer: tokens + primitives | Feature layer: team-specific components)
    - CalloutBox: philosophy ("Lightweight, not restrictive — teams move fast, the system grows organically")

    ---

    **Stop 3: How We Work (5 slides)**

    Slide 1 — "Team Structure":
    - EntityCards variant:yellow: 3 entities (PT = Platform Team, CC = Content/Campaign Team, SP = Safety & Privacy). Each with title and description of responsibilities
    - CalloutBox: future scaling note

    Slide 2 — "Storybook as Component Registry":
    - BulletList variant:default: benefits (discover, prevent duplication, visual testing, collaboration)
    - DataTable variant:default: Storybook structure table (Category | Contents — Foundations, Shared, Platform, Campaign, Feature)

    Slide 3 — "CI/CD & Release Strategy":
    - NumberedSteps variant:red: pipeline evolution steps (Start centralized -> Monitor build times -> Introduce Turborepo if needed -> Run tests for affected modules only)

    Slide 4 — "Testing Strategy":
    - DataTable variant:default: test layers table (Layer | Scope | Tools columns — Unit, Integration, E2E rows)
    - CalloutBox: testing philosophy ("Fast pipelines, high confidence — test what matters, skip what does not")

    Slide 5 — "Privacy-First Analytics":
    - BulletList variant:red: event tracking + guardrails (core events tracked, page views limited, no PII, no persistent identifiers)
    - TwoColumnCards variant:default: 2 cards (Tooling: Plausible or internal pipeline | Compliance: IP anonymization, no PII, aggregated reporting)

    ---

    **Stop 4: Migration (3 slides)**

    Slide 1 — "Migration Strategy":
    - CalloutBox: why incremental (risk, velocity, stability balance)
    - BulletList variant:default: benefits of incremental route migration

    Slide 2 — "Migration Steps":
    - NumberedSteps variant:yellow: 4 steps (Establish Next.js foundation -> Migrate first simple route -> Iterate route by route -> Replace remaining AngularJS sections)

    Slide 3 — "Risk Mitigation":
    - EntityCards variant:red: 3 risks with mitigations (LC = Legacy Complexity, PF = Performance, TC = Team Coordination)

    ---

    **Stop 5: Summary (2 slides)**

    Slide 1 — "What We Are Proposing":
    - BulletList variant:yellow: 5 key takeaways (scalable multi-team dev, rapid campaign iteration, privacy-first analytics, modern performance, safe incremental migration)

    Slide 2 — "Questions?":
    - CalloutBox: single inviting message ("We are ready to walk through any part of the proposal in detail.")
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty && npm test -- src/data/topics/__tests__/stops.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 stop data files rewritten with 18 slides total (4+4+5+3+2). Every slide uses ContentBlock types. TypeScript compiles cleanly. All stops.test.ts assertions pass. No JSX or React imports in any data file.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0 — no TypeScript errors
2. `npm test -- src/data/topics/__tests__/stops.test.ts` — all assertions pass with new block shape
3. `npm test` — full suite runs (pre-existing CarElement/MinifigHead failures are not Phase 4 scope)
4. No React/JSX imports in any `src/data/topics/stop-*.ts` file
</verification>

<success_criteria>
- ContentBlock discriminated union type is exported from presentation.ts
- Slide interface uses `blocks: ContentBlock[]` (not `lines: string[]`)
- BulletList accepts optional heading
- SlideContent renders all 6 block types via switch dispatcher
- All 18 slides authored with real presentation content
- "We" voice, active delivery, editorial sharpness throughout
- New content authored for "Why React + Next.js" and "Experience-First Architecture"
- Variant assignments match CONTEXT.md locked mapping
- TypeScript compiles, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-and-polish/04-01-SUMMARY.md`
</output>
